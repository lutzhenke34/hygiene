name: hygiene_app
description: HACCP Hygiene App

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.0
  supabase_flutter: ^2.5.0
  shared_preferences: ^2.2.0
  file_picker: ^8.1.2
  intl: ^0.19.0   # ✅ HIER gehört es hin
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.9
  riverpod_generator: ^2.4.0
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true